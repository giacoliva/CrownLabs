---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.11.1
  creationTimestamp: null
  name: enrollrequests.crownlabs.polito.it
spec:
  group: crownlabs.polito.it
  names:
    kind: EnrollRequest
    listKind: EnrollRequestList
    plural: enrollrequests
    singular: enrollrequest
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.tenant
      name: Tenant
      type: string
    - jsonPath: .status.valid
      name: Valid
      type: boolean
    name: v1alpha2
    schema:
      openAPIV3Schema:
        description: Tenants can request to be enrolled in a Workspace with an EnrollRequest.
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: EnrollRequestSpec defines the desired state of EnrollRequest
            properties:
              tenant:
                description: Name of the Tenant that wants to be enrolled
                type: string
            required:
            - tenant
            type: object
          status:
            description: EnrollRequestStatus defines the observed state of EnrollRequest
            properties:
              valid:
                description: Whether the EnrollRequest is valid or not
                type: boolean
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
